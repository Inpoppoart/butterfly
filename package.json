{
  "name": "shift-trainer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build step for SHIFT: precompile JSX and inline React so the page loads instantly and works offline.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@babel/core": "7.26.0",
    "@babel/preset-react": "7.26.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
